import styles from "./Spinner.module.scss";

const Spinner = () => {
    return (
        <div className={styles.container}>
            <div>
                <div></div>
            </div>
        </div>
    );
};

export default Spinner;
